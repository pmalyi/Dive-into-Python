Декоратор to_json

Для передачі даних між функціями, модулями або різними системами використовуються формати даних. Одним із найпопулярніших
форматів є JSON. Напишіть декоратор to_json, який можна застосувати до різних функцій, щоб перетворювати їх значення,
що повертається в JSON-формат. Не забудьте зберегти коректне ім'я декорованої функції.

@to_json
def get_data():
   return {
     'data': 42
   }

get_data() # поверне '{"data": 42}'

Опишіть декоратор у файлі та завантажте його на платформу.